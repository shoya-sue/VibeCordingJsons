{
  "$schema": "../../schema.json",
  "description": "フック機能を活用した自動化設定",
  "allowedTools": [
    "view",
    "grep",
    "glob",
    "edit",
    "create",
    "bash",
    "report_progress",
    "code_review"
  ],
  "toolRestrictions": {
    "bash": {
      "allowedCommands": [
        "npm run *",
        "npm test",
        "git *"
      ]
    }
  },
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "echo '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━' && echo '📊 プロジェクト状態' && echo '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━'",
        "matcher": "startup"
      },
      {
        "type": "command",
        "command": "echo '' && echo '🌿 Git ブランチ:' && git branch --show-current",
        "matcher": "startup"
      },
      {
        "type": "command",
        "command": "echo '' && echo '📝 変更ファイル:' && git status --short",
        "matcher": "startup"
      },
      {
        "type": "command",
        "command": "echo '' && echo '📚 最近のコミット:' && git log --oneline --graph -5",
        "matcher": "startup"
      },
      {
        "type": "command",
        "command": "echo '' && echo '🔍 依存関係の状態:' && npm outdated || echo '✅ すべての依存関係が最新です'",
        "matcher": "startup"
      },
      {
        "type": "command",
        "command": "echo '' && echo '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━'"
      }
    ],
    "BeforeEdit": [
      {
        "type": "command",
        "command": "echo '🔍 編集前チェック: Lint実行中...' && npm run lint:check"
      }
    ],
    "AfterEdit": [
      {
        "type": "command",
        "command": "echo '✅ 編集完了: フォーマット確認中...' && npm run format:check"
      }
    ]
  },
  "env": {
    "NODE_ENV": "development",
    "DEBUG": "app:*"
  },
  "llm": {
    "model": "claude-sonnet-4-20250514"
  },
  "notes": [
    "hooks機能を使った自動化の例",
    "SessionStartで、プロジェクトの状態を自動表示",
    "BeforeEdit/AfterEditで、Lint/フォーマットチェックを自動実行",
    "matcherオプションで、起動時のみ実行するフックを制御可能"
  ]
}
