{
  "description": "MCP GitHub Actions Integration - Permissions for CI/CD workflow operations",
  "allowedTools": [
    "github-mcp-server-actions_list",
    "github-mcp-server-actions_get",
    "github-mcp-server-get_job_logs",
    "github-mcp-server-get_workflow_run_usage",
    "github-mcp-server-get_workflow_run_logs_url"
  ],
  "disallowedTools": [],
  "toolRestrictions": {
    "rateLimit": {
      "maxRequestsPerMinute": 30,
      "maxRequestsPerHour": 1000
    },
    "logs": {
      "maxLogSize": 10485760,
      "allowedWorkflows": ["ci.yml", "test.yml", "build.yml"]
    }
  },
  "notes": [
    "Allows GitHub Actions workflow operations",
    "Suitable for CI/CD monitoring and debugging",
    "Log size limited to 10MB to prevent excessive data transfer",
    "Requires GitHub token with actions:read permission",
    "Use with standard configuration for complete development workflow"
  ]
}
