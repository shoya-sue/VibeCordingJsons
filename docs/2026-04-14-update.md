# VibeCording Update — 2026-04-14

## 変更点サマリー

Claude Code v2.1.101 / v2.1.105 の新スラッシュコマンドを CLAUDE.md に追加。
言語別ルール数のカウントミス（8→9言語）を修正。

## 環境スナップショット

- Local ECC installed: `1.9.0`
- Template ECC version: `1.10.0` (install.sh が disk から auto-detect して 1.9.0 に置換)

## トリアージ済みアクションリスト

### 🔴 必須
- [ ] #1: 言語別ルール数を `8 languages × 5 = 40` → `9 languages × 5 = 45` に修正 — `full/CLAUDE.md`

### 🟡 推奨
- [ ] #2: `/team-onboarding` を Slash Commands に追加（v2.1.101）— `full/CLAUDE.md`
- [ ] #3: `/proactive` を Slash Commands に追加（v2.1.105, `/loop` のエイリアス）— `full/CLAUDE.md`

### ⚪ スキップ
- #4: ECC v1.10.0 更新 — 理由: テンプレートは既に 1.10.0。ローカル未インストール版の更新不要
- #5: hooks.md 26 events — 理由: 既に最新（already applied）
- #6: performance.md モデルID — 理由: 既に最新（already applied）
- #7: CLAUDE_CODE_SUBPROCESS_ENV_SCRUB / NO_FLICKER — 理由: 2026-04-13 update で適用済み
- #8: `refreshInterval` (status-line) — 理由: 現テンプレートは command 形式で statusLine を設定済み、追加不要
- #9: `CLAUDE_CODE_USE_MANTLE=1` — 理由: Amazon Bedrock Mantle 不使用環境のため不要

## 変更詳細

### #1: 言語別ルール数修正（🔴 必須）

`full/.claude/rules/ecc/` に 9 言語 (cpp, golang, java, kotlin, php, python, rust, swift, typescript) × 5 ファイルが存在するが、CLAUDE.md は古い「8 languages」のまま。

**Before:**
```
- Rules: `ecc/common/` (10) + language-specific rules (8 languages × 5 = 40)
```

**After:**
```
- Rules: `ecc/common/` (10) + language-specific rules (9 languages × 5 = 45)
```

### #2: `/team-onboarding` 追加（🟡 推奨）

Claude Code v2.1.101 で追加。チームメイト向けのランプアップガイドを自動生成するコマンド。

**After (Slash Commands セクションに追加):**
```
- `/team-onboarding` — チームメイト向けのランプアップガイドを生成
```

### #3: `/proactive` 追加（🟡 推奨）

Claude Code v2.1.105 で追加された `/loop` のエイリアス。

**After (Slash Commands セクションに追加):**
```
- `/proactive` — `/loop` のエイリアス（プロアクティブなループ実行）
```
