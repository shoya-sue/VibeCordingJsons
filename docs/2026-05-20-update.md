# VibeCording Update — 2026-05-20

## 変更点サマリー

Claude Code v2.1.145 (2026-05-19) の変更を反映。本リリースは v2.1.144 同様にバグ修正中心で、テンプレ可視機能としては Stop / SubagentStop フック入力に `background_tasks` と `session_crons` フィールドが追加された点のみが該当する。`claude agents --json`、OTEL span 属性、status line JSON の GitHub repo/PR 情報、Read tool の PARTIAL view fallback はテンプレ設定に直接影響しないためスキップ。ECC は v1.10.0 のまま据置。

## 環境スナップショット

- Local ECC installed: 1.10.0
- Template ECC version: 1.10.0 (in sync — no ECC update needed)
- Current release: v0.41.0
- Next version: v0.42.0
- Local Claude Code: v2.1.145

## 品質分析サマリー

研究対象: Claude Code v2.1.145（2026-05-19、v0.41.0 リリース以降の唯一の新バージョン）。ECC は v1.10.0 が最新で変更なし（2026-04-05 以降新規リリースなし）。

### 候補変更リスト（pre-analysis）

| # | 変更内容 | バージョン |
|---|---------|----------|
| 1 | Stop / SubagentStop フック入力に `background_tasks` と `session_crons` フィールド追加 | v2.1.145 |
| 2 | `claude agents --json` でライブセッションを JSON 出力（tmux-resurrect・ステータスバー・セッションピッカー向け） | v2.1.145 |
| 3 | `agent_id` / `parent_agent_id` 属性が `claude_code.tool` OTEL span に追加 | v2.1.145 |
| 4 | Status line JSON 入力に GitHub repo と PR 情報が含まれるように | v2.1.145 |
| 5 | `/plugin` Discover / Browse 画面でインストール前にコマンド・エージェント・スキル・フック・MCP/LSP サーバーを表示 | v2.1.145 |
| 6 | `claude agents` ターミナルタブタイトルに awaiting-input 件数表示 | v2.1.145 |
| 7 | Slash コマンドと @-mention サジェストでマウスホバー/クリック対応（fullscreen） | v2.1.145 |
| 8 | Read tool が whole-file 読み取りでトークン超過時、エラーではなく "PARTIAL view" 注記付き先頭ページを返すように | v2.1.145 |
| 9 | Bash で非許可環境変数への bare 変数代入が誤って auto-approve されていた permission-prompt bypass 修正 | v2.1.145 |
| 10 | MCP プロンプト slash コマンドが server 検証エラーで raw メッセージを出していた問題を改善（引数名・期待 usage 表示） | v2.1.145 |
| 11 | Skill が `context: fork` 使用時に自己再呼び出しで無限ループする問題を修正 | v2.1.145 |
| 12 | その他多数のバグ修正（spinner freeze、PowerShell resume、Agent Teams 非 ASCII 名、`/review` の deprecated projectCards、`claude plugin validate` で skills エントリがファイル指定の場合を検出、等） | v2.1.145 |

### Lens 1 — Applicability

| # | スコア | 理由 |
|---|--------|------|
| 1 | **Medium-High** | テンプレに `template/.claude/rules/ecc/common/hooks.md` があり Hook Input の各フィールドを文書化している。`background_tasks` / `session_crons` は custom Stop/SubagentStop フックを書くユーザーが直接利用可能なフィールド。`claude --bg` がメインストリーム化した現状、bg タスク状況を Stop hook で確認するユースケースは現実的 |
| 2 | Low | CLI スクリプティング用の特殊機能。template/.claude/settings.json や rules で設定するものではない |
| 3 | Skip | observability 向け OTEL span 属性。テンプレに OTEL 設定なし |
| 4 | Low | template の statusLine は `${HOME}/.claude/statusline.sh` を参照（実体はテンプレ外）。statusline スクリプト自体を template に含めていないので、入力フィールド変更を template ドキュメントに書く必要は薄い |
| 5–7 | Skip | UX 変更、設定変更不要 |
| 8 | Skip | Read tool の動作変更。フォールバックなので既存テンプレに影響なし |
| 9 | Skip | Claude Code 本体のセキュリティ修正。テンプレ allow/deny リストに影響なし |
| 10 | Skip | UX 改善。設定不要 |
| 11 | Skip | Claude Code 本体のバグ修正 |
| 12 | Skip | バグ修正のみ |

### Lens 2 — Cross-file Consistency

- #1 (Stop/SubagentStop hook input fields): `template/.claude/rules/ecc/common/hooks.md` の "Hook Input" 系セクションに追記。README.md のフックテーブル（line 198-220 付近）は event 名と blocking 属性のみで、入力フィールドの詳細は持っていないため README 更新不要。`template/AGENTS.md` も同様にフィールド単位の説明なし。Cross-file 影響は hooks.md のみ

### Lens 3 — Risk Assessment

- #1: **Low**（ドキュメント追加のみ、既存フック動作に影響なし、新フィールドを使わないユーザーは無変更で動作）

### Lens 4 — Completeness

- v0.41.0 までに v2.1.144 までを反映済み
- v2.1.145 のテンプレ可視機能は #1 のみ
- ECC v1.10.0 据置 → `ECC_PLUGIN_ROOT` 変更不要

### 最終トリアージ

🔴 必須: なし（ECC バージョン変更なし、破壊的変更なし）
🟡 推奨: #1 (Stop/SubagentStop hook input フィールド追記)
⚪ スキップ: #2 – #12

## トリアージ済みアクションリスト

### 🔴 必須

（なし）

### 🟡 推奨

- [ ] #1: `background_tasks` と `session_crons` Stop/SubagentStop フック入力フィールドのセクションを追加 — `template/.claude/rules/ecc/common/hooks.md`

### ⚪ スキップ

- #2: `claude agents --json` — 理由: CLI スクリプティング用、テンプレ設定対象外
- #3: OTEL span 属性 — 理由: テンプレに OTEL 設定なし
- #4: Status line JSON の GitHub repo/PR 情報 — 理由: statusline スクリプト実体はテンプレ外
- #5: `/plugin` Discover/Browse 詳細表示 — 理由: UX、設定不要
- #6: `claude agents` タブタイトル — 理由: UX、設定不要
- #7: Slash コマンドマウス対応 — 理由: UX、設定不要
- #8: Read tool PARTIAL view — 理由: フォールバック動作変更、テンプレ影響なし
- #9–#11: 各種バグ・セキュリティ修正 — 理由: Claude Code 本体側、テンプレ allow/deny 不変
- #12: その他バグ修正 — 理由: テンプレ内容に影響なし

## 変更詳細

### #1: Stop/SubagentStop フック入力フィールド追記 — `template/.claude/rules/ecc/common/hooks.md`

**追加位置**: `### フック出力 \`terminalSequence\` フィールド (v2.1.141+)` の直後（"Auto-Accept Permissions" セクションの前）に新規サブセクションを挿入。

**追加内容**:

```markdown
### Stop / SubagentStop フック入力: `background_tasks` と `session_crons` フィールド (v2.1.145+)

Stop / SubagentStop フックの入力 JSON に以下 2 フィールドが含まれる:

- `background_tasks` — 現在実行中のバックグラウンドタスク（`claude --bg` で起動した bg セッションや subagent が spawn した Bash bg プロセス）の配列
- `session_crons` — このセッションでスケジュールされた cron / 定期実行ジョブの配列

`claude --bg` のメインストリーム化に伴い、応答完了時に未完了 bg タスクの有無を判定したり、cron 設定をログ出力したりするユースケースで利用できる:

` ` `jsonc
// command フック内で background_tasks の有無を判定する例
// 入力 JSON: { "background_tasks": [...], "session_crons": [...], ... }
// jq で抽出: cat | jq '.background_tasks | length'
` ` `
```

## Sources

- [Claude Code Changelog](https://code.claude.com/docs/en/changelog)
- [anthropics/claude-code releases](https://github.com/anthropics/claude-code/releases)
- [affaan-m/everything-claude-code releases](https://github.com/affaan-m/everything-claude-code/releases) — no new release (still v1.10.0, latest 2026-04-05)
