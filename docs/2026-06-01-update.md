# VibeCording Update — 2026-06-01

## 変更点サマリー

Claude Code v2.1.157〜v2.1.159 の差分反映。新規の env var 2 つ（`CLAUDE_CODE_ENABLE_AUTO_MODE`、`OTEL_LOG_TOOL_DETAILS`）、dispatched session 用 `agent` settings キー、`/terminal-setup` の挙動拡張をドキュメント化。あわせて本日（2026-06-01）が削除予定日に到達した `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` を「削除済み」へ更新し、テンプレ内で食い違っていたフックイベント数（26→27）と未記載だった `/workflows` を整合させる。ECC は依然 1.10.0（v2.0.0-rc.1 はローカル未導入のため据え置き）。

## 環境スナップショット

- Local ECC installed: **1.10.0**（最新の v2.0.0-rc.1 はローカル未導入）
- Template ECC version: **1.10.0**（`ECC_PLUGIN_ROOT`、変更なし）
- Current release: **v0.49.0**（2026-05-29 / Claude Code v2.1.156 ベース）
- Next release: **v0.50.0**

## 品質分析サマリー

4 レンズ（適合性 / 横断一貫性 / 導入リスク / 適用完全性）で各候補を評価:

- **Lens 1 適合性**: env var・settings キー・slash command 系は設定テンプレに直接価値あり（README env/settings 表に同種項目が既存）。UI 専用 `/config` トグルや startup banner 撤去は適合性 Low → スキップ。
- **Lens 2 一貫性**: フックイベント数が README/hooks.md は「27」、AGENTS.md と repo 直下 CLAUDE.md は「26」で食い違い（MessageDisplay 追加 v2.1.152 の取りこぼし）。→ 27 に統一を 🟡 追加。auto mode env は CLAUDE.md の auto-mode 注記が companion。
- **Lens 3 リスク**: 全項目ドキュメント追記のみで additive、既存挙動変更なし → Low。`OPUS_4_6` 削除は env var を実際に外す挙動変化だが、本日削除日到達済みで記述追従なので安全。
- **Lens 4 完全性**: `/terminal-setup` の説明が GPU accel 無効化の新機能を欠いており不完全 → 更新。dynamic workflows / `/workflows`（v2.1.154）が CLAUDE.md に未記載 → 完全性のため 🟡 追加。

## トリアージ済みアクションリスト

### 🔴 必須

- [ ] #1: `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` 削除日（2026-06-01）到達 — 「削除予定」→「削除済み」へ更新 — `template/.claude/rules/ecc/common/performance.md` + `README.md`

### 🟡 推奨

- [ ] #2: `CLAUDE_CODE_ENABLE_AUTO_MODE=1`（v2.1.158、Bedrock/Vertex/Foundry の Opus 4.7/4.8 で auto mode opt-in）— `README.md` env 表 + `template/CLAUDE.md` Important Notes の auto-mode 行に companion 追記
- [ ] #3: `OTEL_LOG_TOOL_DETAILS=1`（v2.1.157、`tool_decision` telemetry に `tool_parameters` を含める opt-in）— `README.md` env 表
- [ ] #4: `agent` settings キーが dispatched session で honored + `--agent <name>` override（v2.1.157）— `README.md` Other Settings 表 + `template/AGENTS.md` の dispatch フラグ行
- [ ] #5: `/terminal-setup` 説明更新（v2.1.157、VS Code/Cursor/Windsurf 統合ターミナルの GPU acceleration 無効化で文字化け防止も担うように）— `template/CLAUDE.md` Slash Commands
- [ ] #6: `claude plugin init <name>`（v2.1.157、`.claude/skills` にプラグイン scaffold、`.claude/skills` 配下プラグインの marketplace 不要 auto-load）— `README.md` Best Practices に一行
- [ ] #7: フックイベント数 26→27 整合（README/hooks.md は 27、AGENTS.md と repo 直下 CLAUDE.md が 26 のまま）— `template/AGENTS.md` + `CLAUDE.md`（repo 直下）[Lens 2]
- [ ] #8: `/workflows` slash command + dynamic workflows（v2.1.154、CLAUDE.md 未記載）— `template/CLAUDE.md` Slash Commands [Lens 4]

### ⚪ スキップ

- ECC v2.0.0-rc.1 — ローカル未導入（1.10.0）。install.sh はディスクから auto-detect するため、未導入バージョンへテンプレを上げると hooks が壊れる
- v2.1.159 — internal infra のみ、ユーザー向け変更なし
- `/config`「Workflow keyword trigger」トグル — UI 専用トグルで settings.json キーではない。#8 で dynamic workflows を間接カバー
- startup banner 撤去（"bash commands will be sandboxed" / "/ide for…"）— 内部 UI、テンプレ非記載
- v2.1.157 バグ修正群（image paste、sandbox prompt、worktree cleanup、WSL、`/model` picker 等）— テンプレが記述する挙動に影響なし
- `EnterWorktree` mid-session 切替（v2.1.157）— 内部ツール挙動、テンプレ非記載

## 変更詳細

### #1 — `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` 削除済み化

本日 2026-06-01 が changelog（v2.1.154）告知の削除日。

`performance.md` (before):
> `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` は **非推奨（2026-06-01 削除予定）**。Opus 4.8 デフォルト化に伴い廃止

(after):
> `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` は **削除済み（2026-06-01）**。Opus 4.8 デフォルト化に伴い廃止。Opus 4.6 を Fast mode で使うには `/model claude-opus-4-6[1m]` → `/fast on`

`README.md` env 表の同項目も「削除済み（2026-06-01）」へ。

### #2 — `CLAUDE_CODE_ENABLE_AUTO_MODE`

v2.1.158: Auto mode が Bedrock / Vertex / Foundry の Opus 4.7・4.8 で利用可能に（opt-in）。
README env 表へ追加。`template/CLAUDE.md` の「Auto mode は v2.1.152+ でオプトイン同意不要」行に Bedrock/Vertex/Foundry では `CLAUDE_CODE_ENABLE_AUTO_MODE=1` が必要、と companion 追記。

### #3 — `OTEL_LOG_TOOL_DETAILS`

v2.1.157: `tool_decision` telemetry イベントに `tool_parameters`（bash コマンド、MCP/skill 名）を含める opt-in。README env 表へ追加（既存 `OTEL_METRICS_INCLUDE_ENTRYPOINT` と同列）。

### #4 — `agent` settings キー / `--agent`

v2.1.157: `settings.json` の `agent` フィールドが dispatched session で honored に。`--agent <name>` で override。README Other Settings 表へ追加、AGENTS.md の `claude agents` dispatch フラグ列挙に `--agent` を追記。

### #5 — `/terminal-setup` 挙動拡張

v2.1.157: VS Code/Cursor/Windsurf 統合ターミナルで GPU acceleration を無効化し文字化けを防ぐ機能を獲得。CLAUDE.md の説明（現在はスクロール感度設定のみ）に追記。

### #6 — `claude plugin init`

v2.1.157: `.claude/skills` 配下のプラグインが marketplace なしで auto-load、`claude plugin init <name>` で scaffold。README Best Practices に一行（`claude plugin prune` / `claude project purge` と同列）。

### #7 — フックイベント数 26→27

MessageDisplay（v2.1.152、27 個目）追加時に README/hooks.md は更新済みだが AGENTS.md（"26 event hooks"）と repo 直下 CLAUDE.md（"all 26 events"）が未追従。両者を 27 に統一。

### #8 — `/workflows` + dynamic workflows

v2.1.154 の dynamic workflows（数十〜数百エージェントをバックグラウンドでオーケストレーション）と閲覧用 `/workflows` が CLAUDE.md Slash Commands に未記載。一行追加。
</content>
</invoke>
