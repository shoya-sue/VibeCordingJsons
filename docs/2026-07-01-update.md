# VibeCording Update — 2026-07-01

## 変更点サマリー

Claude Code **2.1.197** で **Claude Sonnet 5** が Claude Code の新デフォルトモデルに昇格した（native 1M-token context window、8/31 まで $2/$10 per Mtok のプロモ価格、model id `claude-sonnet-5`）。これに伴い `sonnet` エイリアスの解決先が Sonnet 4.6 → Sonnet 5 に変わり、テンプレのモデル記述（"Sonnet 4.6" 名指し、"Opus 4.8 = デフォルト" の主張）が factually stale になった。ドキュメント精度更新（doc-only）。テンプレは `settings.json` で `"model": "opus"` を固定しているため、デフォルト flip による**出荷 config の破壊はゼロ**。

## 環境スナップショット

- Local ECC installed: **2.0.0**
- Template ECC version: **2.0.0**（一致・変更なし）
- Current release: **v0.75.0**（Claude Code 2.1.196 カバー）
- Latest Claude Code: **2.1.197**

## 品質分析サマリー

- **Lens 1 Applicability**: モデルデフォルト変更は config テンプレにとって high（`/model` / settings.json の `model` キー・opusplan ワークフローに直結）。プロモ価格は時限（8/31）で evergreen roster には不適 → README の version-anchored `>` ノートにのみ載せる。
- **Lens 2 Cross-file**: "Sonnet 4.6" は `performance.md` と `README.md` の両方に存在。"Opus 4.8 = デフォルト" の主張は `performance.md`。整合のため `template/CLAUDE.md` Important Notes にも 1 行 companion を追加（README/performance と足並みを揃える）。
- **Lens 3 Risk**: doc-only・additive。テンプレは `model: opus` を明示固定するためデフォルト flip の影響なし。プロモ価格は version-anchored ノートに閉じ込め将来の staleness を局所化。Risk = Low。
- **Lens 4 Completeness**: "Sonnet 4.6" 全 3 箇所（performance.md:10 / README.md:394 / README.md:401）を洗い出し済み。`settings.json:161` の `"model": "opus"` は意図的な固定なので**変更しない**（テンプレ設計判断）。root/template AGENTS.md・root CLAUDE.md にモデル版数の参照なし（grep 済み）。
- **Lens 5 Breaking-change audit**: 2.1.197 は matcher / permission / glob / parser のセマンティクスを一切変更しない（唯一の項目はモデル追加）。出荷 hook matcher・permission ルールへの影響なし。デフォルトモデル flip も `settings.json` の明示 `"model": "opus"` により無効化される。→ **出荷 config 破壊なし**。詳細は下記「破壊的変更チェック」。

## トリアージ済みアクションリスト

### 🔴 必須
- [ ] #1: `sonnet` エイリアスの解決先が Sonnet 5 に変更（Sonnet 4.6 名指しが stale）+ Claude Code デフォルトが Sonnet 5 に flip — `template/.claude/rules/ecc/common/performance.md`, `README.md`, `template/CLAUDE.md`（companion）

### 🟡 推奨
- [ ] #2: Sonnet 5 は native 1M-token context 標準内蔵（Fable 5 と同様 `[1m]` 不要）。デフォルト Sonnet 5 利用者は暗黙的に 1M セッションになる点を context 節に補足 — `template/.claude/rules/ecc/common/performance.md`
- [ ] #3: プロモ価格 $2/$10 per Mtok（8/31 まで）を README のモデル表直後の version-anchored `>` ノートに記載（時限情報なので evergreen roster には載せない）— `README.md`

### ⚪ スキップ
- #4: ECC 新バージョン — なし（最新 2.0.0、ローカルインストール済み・テンプレ一致）。変更なし。
- #5: `settings.json` の `"model": "opus"` を Sonnet 5 に変える — **しない**。テンプレは意図的に Opus/opusplan を推奨する設計判断。デフォルト flip は docs 精度の問題であり出荷選択の変更ではない。

## 破壊的変更チェック（Lens 5）

| 対象 | 新セマンティクスの影響 | 判定 |
|------|----------------------|------|
| 出荷 hook matcher（`settings.json`） | 2.1.197 は matcher 評価規則を変更しない | 壊れない |
| permission ルール（allow/ask/deny） | 変更なし | 壊れない |
| `settings.json` `"model": "opus"` | 明示指定のためデフォルト flip（→ Sonnet 5）を無効化 | 壊れない |
| `opusplan` 推奨 | execution フェーズの `sonnet` が Sonnet 5 に解決されるだけ（自動的に改善、config 変更不要） | 壊れない |

## 変更詳細

### #1 Sonnet 5 デフォルト化（🔴）

**performance.md — Model Selection Strategy**
- Before: `**Sonnet 4.6** (Best coding model):`
- After: `**Sonnet 5** (v2.1.197+ の Claude Code デフォルト、best coding model、model id `claude-sonnet-5`、1M コンテキスト標準内蔵):`
- Before: `**Opus 4.8** (Deepest reasoning, v2.1.154+ のデフォルト):`
- After: `**Opus 4.8** (Deepest reasoning、v2.1.154–2.1.196 の CC デフォルト。v2.1.197+ でデフォルトは Sonnet 5 に。テンプレは settings.json で opus 固定):`

**README.md — モデル表**
- `sonnet` 行: `Sonnet 4.6 (balanced)` → `Sonnet 5 (v2.1.197+ の Claude Code 既定・1M context 標準内蔵・claude-sonnet-5)`
- `opus` 行: `(highest performance, default)` → `(highest performance; テンプレ既定 = settings.json で opus 固定)`
- opusplan workflow: `switches to Sonnet 4.6` → `switches to Sonnet 5`

**template/CLAUDE.md — Important Notes（companion 1 行）**
- 追加: Sonnet 5 が v2.1.197+ の Claude Code デフォルト（native 1M context、`claude-sonnet-5`）。テンプレは `settings.json` で opus 固定 + settings.local.json で opusplan 推奨のため既定挙動は不変。

### #2 Sonnet 5 の 1M context（🟡）
performance.md の context 節（#43989 ワークアラウンド周辺）に、Sonnet 5 / Fable 5 は `[1m]` サフィックス無しで 1M を標準提供し、デフォルト Sonnet 5 利用者は暗黙的に 1M セッションになる旨を補足。

### #3 プロモ価格（🟡, README のみ）
README モデル表直後に version-anchored `>` ノートを追加:
`> **v2.1.197+**: Claude Sonnet 5 が Claude Code の既定モデルに（native 1M context、model id claude-sonnet-5）。導入当初は 8/31 まで $2/$10 per Mtok のプロモ価格。テンプレは settings.json で opus を固定するため、既定を Sonnet 5 にしたい場合は model を外すか sonnet を指定。`
