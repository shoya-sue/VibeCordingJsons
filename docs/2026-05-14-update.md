# VibeCording Update — 2026-05-14

## 変更点サマリー

Claude Code v2.1.141 (2026-05-13) の変更を反映。新フック出力フィールド `terminalSequence`（デスクトップ通知・ウィンドウタイトル・ベル制御）と、新環境変数 `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` / `ANTHROPIC_WORKSPACE_ID` を template に追加。ECC バージョン変更なし。

## 環境スナップショット

- Local ECC installed: 1.10.0
- Template ECC version: 1.10.0 (in sync — no ECC update needed)
- Current release: v0.35.3
- Next version: v0.36.0

## 品質分析サマリー

研究対象: Claude Code v2.1.141（2026-05-13 23:19 UTC、v0.35.3 リリース以降の唯一の新バージョン）

### 候補変更リスト（pre-analysis）

| # | 変更内容 | バージョン |
|---|---------|----------|
| 1 | フック出力 JSON の `terminalSequence` フィールド | v2.1.141 |
| 2 | 環境変数 `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` | v2.1.141 |
| 3 | 環境変数 `ANTHROPIC_WORKSPACE_ID` | v2.1.141 |
| 4 | CLI フラグ `claude agents --cwd <path>` | v2.1.141 |
| 5 | `/feedback` で最近 24h/7d セッションを添付可能 | v2.1.141 |
| 6 | Rewind メニュー「Summarize up to here」 | v2.1.141 |
| 7 | Auto mode 許可ダイアログが `permissions.ask` 理由を表示 | v2.1.141 |
| 8 | File-edit 権限プロンプトに "view diff in your IDE" 復活 | v2.1.141 |
| 9 | `/bg`・`←←` のバックグラウンド化が現 permission mode を保持 | v2.1.141 |
| 10 | 多数のバグ修正（model 同期、apiKeyHelper 漏れ、MCP 認証など） | v2.1.141 |

### Lens 1 — Applicability

| # | スコア | 理由 |
|---|--------|------|
| 1 | High | カスタムフック作者がデスクトップ通知/タイトル/ベルを実装するための新フィールド |
| 2 | Medium | SSH をブロックするネットワーク環境のユーザに有用な env オプション |
| 3 | Low | エンタープライズ向け workload identity federation。テンプレートに記載は最小限で良いが load-bearing ではない |
| 4 | Low | 操作的 CLI フラグ、テンプレート設定ではない |
| 5 | Skip | 操作的 UI 機能、設定/テンプレート対象外 |
| 6 | Skip | 操作的 UI 機能 |
| 7 | Skip | 操作的 UI 機能（既存 `autoMode` 設定の説明強化のみ） |
| 8 | Skip | 操作的 UI 機能 |
| 9 | Skip | 操作的 UI 機能 |
| 10 | Skip | ユーザ可視のテンプレート内容に影響しないバグ修正 |

### Lens 2 — Cross-file Consistency

- #1 `terminalSequence`: `template/.claude/rules/ecc/common/hooks.md` の Advanced Features セクション。`README.md` の hooks セクションには列挙していないので追加不要
- #2 `CLAUDE_CODE_PLUGIN_PREFER_HTTPS`: `README.md` の env テーブルのみ（他のプラグイン関連 env 変数と同じ扱い）
- #3 `ANTHROPIC_WORKSPACE_ID`: `README.md` の env テーブルのみ

### Lens 3 — Risk Assessment

- 全 🟡 項目: **Low**（ドキュメント追加のみ、既存動作不変、Opt-in な機能）

### Lens 4 — Completeness

- v0.35.3 までに v2.1.140 までを既反映済み（前回 2026-05-13 update 参照）
- v2.1.141 の新規可視機能は 3 項目のみ。それ以外は操作的 UI 拡張・バグ修正でテンプレ反映対象外

### 最終トリアージ

🔴 必須: なし（ECC バージョン変更なし、破壊的変更なし）
🟡 推奨: #1, #2, #3
⚪ スキップ: #4, #5, #6, #7, #8, #9, #10

## トリアージ済みアクションリスト

### 🔴 必須

（なし）

### 🟡 推奨

- [ ] #1: `terminalSequence` フック出力フィールドを Advanced Features に追加 — `template/.claude/rules/ecc/common/hooks.md`
- [ ] #2: `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` env 変数を追加 — `README.md`
- [ ] #3: `ANTHROPIC_WORKSPACE_ID` env 変数を追加 — `README.md`

### ⚪ スキップ

- #4: `claude agents --cwd <path>` — 理由: 操作的 CLI フラグ、テンプレート設定ではない（`claude agents` 自体は AGENTS.md に既記載）
- #5: `/feedback` 24h/7d セッション添付 — 理由: 操作的 UI 機能
- #6: Rewind「Summarize up to here」— 理由: 操作的 UI 機能
- #7: Auto mode 許可ダイアログ理由表示 — 理由: 操作的 UI 機能（既存 `autoMode.hard_deny` 設定で十分カバー済み）
- #8: "view diff in your IDE" 復活 — 理由: 操作的 UI 機能
- #9: `/bg`/`←←` の permission mode 保持 — 理由: 操作的 UI 機能
- #10: バグ修正（model 同期、apiKeyHelper、MCP 認証等）— 理由: テンプレート内容に影響なし

## 変更詳細

### #1: `terminalSequence` フック出力フィールド — `template/.claude/rules/ecc/common/hooks.md`

**before**: `## Advanced Features` 配下の最後が `### Hook Input: effort.level フィールドと $CLAUDE_EFFORT 環境変数 (v2.1.133+)`

**after**: その後に新セクションを追加

```markdown
### フック出力 `terminalSequence` フィールド (v2.1.141+)

フック JSON 出力に `terminalSequence` フィールドを返すと、ターミナルを乗っ取らずに
デスクトップ通知・ウィンドウタイトル・ベル等のエスケープシーケンスを発行できる。
通知系・ステータス可視化系フックでスクリーン更新を干渉せずに使える。

```jsonc
// 例: ターン完了時にデスクトップ通知（OSC 9）とウィンドウタイトル更新（OSC 2）を行う
{
  "hookSpecificOutput": {
    "terminalSequence": "]9;Task complete]2;claude:done"
  }
}
```
```

### #2: `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` — `README.md`

**before**: env 変数テーブルの末尾は `CLAUDE_PROJECT_DIR`

**after**: `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` を追加

```markdown
| `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` | GitHub からのプラグインソース取得を SSH ではなく HTTPS で行う（SSH ブロック環境向け）（v2.1.141+） | `1` |
```

### #3: `ANTHROPIC_WORKSPACE_ID` — `README.md`

**before**: env 変数テーブル末尾（#2 適用後）

**after**: `ANTHROPIC_WORKSPACE_ID` を追加

```markdown
| `ANTHROPIC_WORKSPACE_ID` | Workload identity federation 用のワークスペース ID（エンタープライズ向け）（v2.1.141+） | (set if applicable) |
```
