# VibeCording Update — 2026-06-22

## 変更点サマリー

v0.65.0（Claude Code 2.1.183 までカバー）以降の新規 Claude Code エントリは **2.1.185 の 1 件のみ**（2.1.184 は欠番）。内容は stream-stall ヒントの文言とタイミング変更で、設定不可な CLI 内部の runtime UX 詳細のため、4 レンズ分析の結果 **テンプレートへの適用対象なし**。したがって本リリースは **0 変更カバレッジ記録リリース（doc-only）** とし、バージョン追跡の連続性を維持する。ECC は 2.0.0 のまま不変。

## 環境スナップショット

- Local ECC installed: **2.0.0**（`~/.claude/plugins/cache/everything-claude-code/ecc/2.0.0`、legacy `everything-claude-code/1.10.0` も併存）
- Template ECC version: **2.0.0**（`template/.claude/settings.json` の `ECC_PLUGIN_ROOT`、変更不要）
- Current release: **v0.65.0**（Claude Code 2.1.183 までカバー）
- Latest Claude Code: **v2.1.185**（2.1.184 は欠番 = `release not found`）
- Latest ECC: **2.0.0**（2026-06-10、変更なし）

## 品質分析サマリー

### 候補変更: Claude Code 2.1.185 — stream-stall ヒント文言＋タイミング変更

> stream-stall ヒントが "No response from API · Retrying in …" から **"Waiting for API response · will retry in …"** に変更され、発火が 10 秒の沈黙後から **20 秒後** に変更された。

| レンズ | 評価 | 根拠 |
|--------|------|------|
| **L1 Applicability** | **Skip** | 設定キー / hook 入力フィールド / env var / CLI フラグのいずれでもない。CLI が API 応答待ちの間に表示する内部メッセージ文言とタイミングのみで、テンプレート利用者が設定・参照できる surface が存在しない。 |
| **L2 Cross-file Consistency** | 該当なし | `grep -rinE "stream-stall\|retry\|No response from API\|Waiting for API\|stall"` を `template/` / `README.md` / `docs/` に実行 → stream-stall ヒントへの参照ゼロ（hooks.md の `retry: true` は無関係な `PermissionDenied` フックフィールド）。横断更新が必要なファイルなし。 |
| **L3 Risk** | Low | テンプレートに記述しないため、利用者への影響ゼロ。 |
| **L4 Completeness** | 完全 | 2.1.185 の changelog エントリはこの 1 件のみ。見落とした関連項目なし。template / README いずれにも追記不要。 |

**トリアージ結論**: ⚪ スキップ（Applicability=Skip）。

### ECC バージョン

- Local 最高 = 2.0.0、template も既に 2.0.0 → 変更不要（⚪）。

## トリアージ済みアクションリスト

### 🔴 必須
- なし

### 🟡 推奨
- なし

### ⚪ スキップ
- #1: Claude Code 2.1.185 — stream-stall ヒント文言＋タイミング変更（10s→20s） — 理由: 設定不可な CLI 内部 runtime UX。設定キー/hook/env var/CLI フラグのいずれでもなく、テンプレート利用者が設定・参照できる surface なし（L1 Applicability=Skip）。template/README 双方に参照ゼロ（L2）。
- #2: ECC 2.0.0 — 理由: template が既に 2.0.0 を指しており差分なし。
- #3: Claude Code 2.1.184 — 理由: 欠番（`gh release view v2.1.184` → `release not found`）。

## 変更詳細

本リリースで変更したテンプレートファイルは **なし**。docs/2026-06-22-update.md（本ファイル）のみ追加し、2.1.185 のレビュー結果と「適用対象ゼロ」を記録する 0 変更カバレッジ記録リリースとする。
