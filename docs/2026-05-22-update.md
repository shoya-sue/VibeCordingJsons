# VibeCording Update — 2026-05-22

## 変更点サマリー

Claude Code v2.1.146 / v2.1.147 / v2.1.148（2026-05-21〜22）の変更を反映。3 リリースとも内容はバグ修正と UX 改善が中心で、テンプレ可視機能としては **`/simplify` → `/code-review` への slash コマンドリネーム**（effort レベル指定と `--comment` フラグ対応）のみが該当する。ECC は v1.10.0 のまま据置。

## 環境スナップショット

- Local ECC installed: 1.10.0
- Template ECC version: 1.10.0 (in sync — no ECC update needed)
- Current release: v0.42.0
- Next version: v0.43.0
- Local Claude Code: v2.1.148

## 品質分析サマリー

研究対象: Claude Code v2.1.146（2026-05-21）/ v2.1.147（2026-05-21）/ v2.1.148（2026-05-22）。ECC は v1.10.0 が最新で変更なし。

### 候補変更リスト（pre-analysis）

| # | 変更内容 | バージョン |
|---|---------|----------|
| 1 | `/simplify` を `/code-review` にリネーム。effort 引数 (`/code-review high`) と `--comment` フラグ（GitHub PR inline コメント投稿）対応。旧 cleanup-and-fix 動作は削除 | v2.1.146 / v2.1.147 |
| 2 | Pinned background sessions（`Ctrl+T` in `claude agents`）が idle で生存維持、Claude Code 更新を in-place で適用、memory pressure 時は non-pinned セッションが先に shed | v2.1.147 |
| 3 | Auto-updater 改善: transient network failure リトライ、エラー分類/OS エラーコード表示、失敗時に現バージョン表示 | v2.1.147 |
| 4 | Prompt history で連続重複エントリを記録しない（↑キーで recall→再送で複製されない） | v2.1.147 |
| 5 | Plugin agents の `tools:` frontmatter に複数 `Agent(...)` 型を宣言すると最後のエントリ以外が dropped されるバグを修正 | v2.1.147 |
| 6 | Hook `if` 条件で `PowerShell(git push*)` 等の引数付きパターンが never matching だったバグを修正（旧: `PowerShell(*)` のみ動作） | v2.1.147 |
| 7 | PowerShell tool が default formatter コマンドで出力を drop していたバグを修正 | v2.1.147 |
| 8 | `CLAUDE_CODE_SUBAGENT_MODEL` が Agent Teams で spawn された teammate プロセスに適用されないバグを修正 | v2.1.147 |
| 9 | Diff rendering performance 改善（大規模ファイル編集） | v2.1.147 |
| 10 | Enterprise login restriction (`forceLoginOrgUUID` / `forceLoginMethod`) が third-party-provider/API-key セッションで enforce されないバグを修正 | v2.1.147 |
| 11 | `!` command 出力中の `&` が `&amp;` 表示されて URL コピペで壊れるバグを修正 | v2.1.147 |
| 12 | Unknown slash command が headless/SDK モードで silent fail だったのを error 表示に変更 | v2.1.147 |
| 13 | Shell snapshot がアンダースコア先頭の user function を drop していたバグを修正 | v2.1.147 |
| 14 | PowerShell tool: winget / Microsoft Store install 時 exit 1 失敗、winget 系全般を修正 | v2.1.146 / v2.1.147 |
| 15 | MCP server pagination が page 1 以降の resources/templates/prompts を drop していたバグを修正 | v2.1.147 |
| 16 | `/effort` のスライダーが現在の effort で開かないバグを修正 | v2.1.147 |
| 17 | Bash tool が全コマンドで exit code 127 を返す regression（v2.1.147 から）を修正 | v2.1.148 |
| 18 | その他多数のバグ修正（auto mode AskUserQuestion 抑制、`/theme` Esc、Windows NTFS junction、Agent SDK uncaught exception、pasted text placeholder、GNOME Terminal middle-click paste 等） | v2.1.146-148 |

### Lens 1 — Applicability（テンプレート適合性）

| # | スコア | 理由 |
|---|--------|------|
| 1 | **Medium** | テンプレ `template/CLAUDE.md` の "Slash Commands" セクションに記載すべき。`/code-review` は built-in slash command（旧 `/simplify` のリネーム）で、effort 引数と `--comment` で PR コメント投稿という新機能を持つため、テンプレ利用者の発見性が低い |
| 2 | Low | `claude agents` UX 改善。テンプレ設定対象外 |
| 3 | Low | Auto-updater は CLI 内部動作、テンプレ設定対象外 |
| 4 | Skip | UX 改善、透過的 |
| 5 | Low | テンプレ内 agent 定義（`code-explorer.md`, `code-reviewer.md`）は単一 `Agent(...)` 型のみのため影響なし。複数型を使うパターンが推奨されるわけでもないので doc 追記不要 |
| 6 | Low | `template/.claude/rules/ecc/common/hooks.md` の "Conditional Filtering" セクションは `if: "Bash(git *)"` パターンを示すだけ。PowerShell 限定のバグだったため、 Windows ユーザー向けに 1 行注記する価値はあるが、テンプレが Windows 中心ではないため Low |
| 7 | Skip | PowerShell tool バグ修正、テンプレ設定対象外 |
| 8 | Low | テンプレ `CLAUDE.md` で `teammateMode: auto` を有効化しているが、`CLAUDE_CODE_SUBAGENT_MODEL` 自体はテンプレ未言及。バグ修正のため doc 追記不要 |
| 9 | Skip | パフォーマンス改善、透過的 |
| 10–18 | Skip | バグ修正 / セキュリティ修正 / UX、テンプレ設定対象外 |

### Lens 2 — Cross-file Consistency

- #1（`/code-review` built-in slash command）:
  - `template/CLAUDE.md` の "Slash Commands" セクションに追記必須
  - README.md の "Slash Commands" 表（行 200 周辺）も照会対象だが、template/CLAUDE.md の slash commands 一覧をミラーするセクションは README に存在しない（README は機能サマリーのみ）→ 更新不要
  - `template/.github/copilot-instructions.md` は GitHub Copilot 向けで Claude Code slash command 一覧を持たない→ 更新不要
  - **既存の "Code review" 関連エントリ**（`template/.claude/rules/ecc/common/code-review.md`, `template/.claude/agents/code-reviewer.md` 等）は `code-reviewer` "agent" を指しており、新しい `/code-review` "slash command" とは別物。混同しないよう、`/code-review` 追記時には built-in slash command であることを明示

### Lens 3 — Risk Assessment

- #1: **Low**（ドキュメント追加のみ、既存ユーザーは無変更で動作）
- #6: Low（doc 追記しないため）

### Lens 4 — Completeness

- v0.42.0 までに v2.1.145 まで反映済み
- v2.1.146-148 のテンプレ可視機能は #1 のみ
- ECC v1.10.0 据置 → `ECC_PLUGIN_ROOT` 変更不要

### 最終トリアージ

🔴 必須: なし（ECC バージョン変更なし、破壊的変更なし）
🟡 推奨: #1 (`/code-review` slash command を CLAUDE.md に追記)
⚪ スキップ: #2 – #18

## トリアージ済みアクションリスト

### 🔴 必須

（なし）

### 🟡 推奨

- [ ] #1: `/code-review [effort] [--comment]` を Slash Commands セクションに追加 — `template/CLAUDE.md`

### ⚪ スキップ

- #2: Pinned background sessions — 理由: `claude agents` UX 改善、テンプレ設定対象外
- #3: Auto-updater 改善 — 理由: CLI 内部動作、テンプレ設定不要
- #4: Prompt history 重複抑制 — 理由: UX、透過的
- #5: Plugin agents の `tools:` 複数 Agent 型 — 理由: テンプレ内 agent 定義は単一型のみ
- #6: Hook `if` PowerShell バグ修正 — 理由: PowerShell 限定の修正、テンプレ doc は Bash 例示
- #7: PowerShell formatter 出力 — 理由: バグ修正、設定対象外
- #8: `CLAUDE_CODE_SUBAGENT_MODEL` teammate — 理由: バグ修正、テンプレで未言及
- #9: Diff rendering — 理由: パフォーマンス改善、透過的
- #10–#18: その他バグ・セキュリティ修正 — 理由: Claude Code 本体側、テンプレ allow/deny 不変

## 変更詳細

### #1: `/code-review` slash command を追加 — `template/CLAUDE.md`

**追加位置**: "Slash Commands" セクション内、`/ultrareview` の直後（同じくコードレビュー系のため近接配置）。

**追加内容**:

```markdown
- `/code-review [effort] [--comment]` — 現在の diff のバグを effort レベル指定でレビュー（v2.1.146+ で `/simplify` から名称変更）。`low|medium` は high-confidence findings のみ、`high|max` で broader coverage。`--comment` で GitHub PR にインラインコメント投稿（`/ultrareview` よりも軽量、ローカルブランチでも動作）
```

旧 `/simplify`（cleanup-and-fix 動作）は削除されたため、本テンプレに `/simplify` の言及はもともとなく削除対象なし。

## Sources

- [Claude Code Changelog](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)
- [anthropics/claude-code releases](https://github.com/anthropics/claude-code/releases)
- [affaan-m/everything-claude-code releases](https://github.com/affaan-m/everything-claude-code/releases) — no new release (still v1.10.0, latest 2026-04-05)
