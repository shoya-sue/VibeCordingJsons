# VibeCording Update — 2026-06-03

## 変更点サマリー

前回リリース v0.50.0（2026-06-01、CC v2.1.157–159 まで反映）以降に出た **Claude Code v2.1.160 / v2.1.161** を反映。最重要は **dynamic-workflow 起動キーワードの `workflow` → `ultracode` への変更**（v2.1.160）で、現行テンプレートの記述が事実誤りになっている。加えて v2.1.160 のビルトイン安全プロンプト、v2.1.161 の `OTEL_RESOURCE_ATTRIBUTES` メトリクスラベルを文書反映。ECC は最新安定版 v1.10.0 のまま（v2.0.0-rc.1 は pre-release かつローカル未インストール → スキップ）。

## 環境スナップショット

- Local ECC installed: `1.10.0`（最新安定版。`v2.0.0-rc.1` は pre-release、未インストール）
- Template ECC version: `1.10.0`（`ECC_PLUGIN_ROOT`、変更なし）
- Current release: `v0.50.0`（2026-06-01）

## 品質分析サマリー

| 項目 | Lens1 適合性 | Lens2 横断 | Lens3 リスク | Lens4 完全性 | 判定 |
|---|---|---|---|---|---|
| #1 workflow→ultracode 改名 | High（既存記述が事実誤り） | `template/CLAUDE.md:67` のみ（repo 全文 grep 確認、README/AGENTS/copilot は未言及） | Low（doc-only・誤り訂正） | 全 1 箇所カバー | 🔴 |
| #2 ビルトイン安全プロンプト（shell 起動ファイル + acceptEdits ビルド設定） | Medium（least-privilege テンプレに有用な安全挙動） | CLAUDE.md Important Notes が定位置、companion 不要 | Low（doc-only・追記） | v2.1.160 の 2 安全項目を 1 行に集約 | 🟡 |
| #3 `OTEL_RESOURCE_ATTRIBUTES` メトリクスラベル | Medium（README に既存 OTEL 群あり、enterprise telemetry 向け） | README env 表のみ、companion 不要 | Low（doc-only・追記） | OTEL 群に追記で完結 | 🟡 |
| ECC v2.0.0-rc.1 | — | — | High（未インストール版に上げると hook 破損） | — | ⚪ |
| `OPUS_4_6` override 削除 | — | — | — | 既に v0.50.0 で反映済（performance.md / README） | ⚪ |
| Edit: grep が read-before-edit を満たす | Low（ランタイム挙動・設定面なし） | — | — | — | ⚪ |
| 並列 Bash 失敗の分離 / `claude agents` done/total / `/mcp` 折りたたみ / `claude mcp` secret 秘匿 / Reduce motion | Skip（UI・ランタイム、テンプレ設定面なし） | — | — | — | ⚪ |

## トリアージ済みアクションリスト

### 🔴 必須
- [ ] #1: dynamic-workflow 起動キーワードを `workflow` → `ultracode` に訂正（v2.1.160） — `template/CLAUDE.md`

### 🟡 推奨
- [ ] #2: v2.1.160 ビルトイン安全プロンプト（shell 起動ファイル / acceptEdits ビルドツール設定）を 1 行追記 — `template/CLAUDE.md`（Important Notes）
- [ ] #3: `OTEL_RESOURCE_ATTRIBUTES`（v2.1.161、メトリクスラベル）を env 表に追記 — `README.md`

### ⚪ スキップ
- ECC `v2.0.0-rc.1` — 理由: pre-release かつローカル未インストール。`install.sh` はディスクから自動検出するため、未インストール版に `ECC_PLUGIN_ROOT` を上げると hook が壊れる
- `CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE` 削除（06-01） — 理由: v0.50.0 で反映済み（`performance.md` / `README.md` 両方）
- Edit tool: 単一ファイル `grep`/`egrep`/`fgrep` が read-before-edit を満たす（v2.1.160） — 理由: ランタイム挙動、テンプレ設定面なし
- 並列 Bash 失敗が他呼び出しを巻き込まない（v2.1.161） — 理由: ランタイム挙動、設定面なし
- `claude agents` done/total 表示 / `/mcp` 未使用コネクタ折りたたみ / Reduce motion 対応（v2.1.161） — 理由: UI、設定面なし
- `claude mcp list/get/add` の secret 秘匿（v2.1.161） — 理由: 自動挙動。テンプレは既に `${ENV_VAR}` 方式を明記済みで設定変更不要

## 変更詳細

### #1 workflow → ultracode（v2.1.160）

changelog 原文（v2.1.160）:
> Renamed the dynamic-workflow trigger keyword from `workflow` to `ultracode`. The word "workflow" no longer triggers a run; asking for one in your own words still works. The trigger keyword is highlighted in violet in the prompt input

`template/CLAUDE.md:67`

Before:
```
- `/workflows` — dynamic workflow の実行状況を表示（v2.1.154+）。プロンプトで「workflow」と依頼すると数十〜数百エージェントをバックグラウンドでオーケストレーションする dynamic workflow が起動。`/config` の「Workflow keyword trigger」でキーワード起動を無効化可
```

After:
```
- `/workflows` — dynamic workflow の実行状況を表示（v2.1.154+）。プロンプトに `ultracode` キーワードを含めると数十〜数百エージェントをバックグラウンドでオーケストレーションする dynamic workflow が起動（v2.1.160 で起動キーワードが `workflow` から `ultracode` に変更。「workflow」という語ではもう起動しない）。`/config` の「Workflow keyword trigger」でキーワード起動を無効化可
```

### #2 ビルトイン安全プロンプト（v2.1.160）

changelog 原文（v2.1.160）:
> Added a prompt before writing to `.zshenv`, `.zlogin`, `.bash_login`, and `~/.config/git/`
> `acceptEdits` mode now prompts before writing build-tool config files that grant code execution (`.npmrc`, `.yarnrc*`, `bunfig.toml`, `.bazelrc`, `.pre-commit-config.yaml`, `.devcontainer/`, etc.)

`template/CLAUDE.md` Important Notes に 1 行追記:
```
- v2.1.160+ ではシェル起動ファイル（`.zshenv` / `.zlogin` / `.bash_login` / `~/.config/git/`）への書き込み、および `acceptEdits` モードでのビルドツール設定ファイル（`.npmrc` / `.yarnrc*` / `bunfig.toml` / `.bazelrc` / `.pre-commit-config.yaml` / `.devcontainer/` 等、コード実行を許す設定）書き込み前に確認プロンプトが入る（ビルトイン安全策、settings.json 設定不要）
```

### #3 OTEL_RESOURCE_ATTRIBUTES（v2.1.161）

changelog 原文（v2.1.161）:
> `OTEL_RESOURCE_ATTRIBUTES` values are now included as labels on metric datapoints, so you can slice usage metrics by custom dimensions (team, repo, etc.)

`README.md` env 表（OTEL 群、line 256 直後）に追記:
```
| `OTEL_RESOURCE_ATTRIBUTES` | OpenTelemetry メトリクスのデータポイントにカスタムラベル（team・repo 等の任意ディメンション）を付与（v2.1.161+） | `team=infra,repo=app` |
```
