# VibeCording Update — 2026-07-02

## 変更点サマリー

Claude Code 2.1.198 カバレッジ。目玉は **Explore agent のデフォルトモデルが haiku 固定 → メインセッションのモデル継承（≤opus）に変更**され、テンプレの複数箇所の「Explore = haiku」記載が不正確化したこと（訂正必須）。加えて `claude agents` 起動の background agent の auto-commit/push/draft-PR、bundled `/dataviz` skill、`Notification` hook の background agent 発火、subagent / context compaction の extended thinking 継承を追記。

## 環境スナップショット

- Local ECC installed: 2.0.0（ecc dir）/ 1.10.0（legacy dir）
- Template ECC version: 2.0.0
- Current release: v0.78.0
- Latest Claude Code: 2.1.198

## 品質分析サマリー（5 レンズ）

- **Lens 1 Applicability**: Explore モデル変更＝High（テンプレ既存記載の誤り訂正）。background agent 自動化・`/dataviz`・Notification 発火・subagent thinking 継承＝Medium。AWS failover・各種 UI bugfix＝Low/Skip。
- **Lens 2 Cross-file**: Explore モデル変更は `subagent-delegation.md` / `README.md` / `obsidian-mcp.md` / `performance.md` の 4 ファイル横断。すべて companion 更新。
- **Lens 3 Risk**: 全て doc-only。Explore 記載訂正は「誤情報の除去」で Low。破壊的挙動変更なし。
- **Lens 4 Completeness**: `Explore = haiku` は 3 箇所に散在 → 全て訂正し、`performance.md` に根拠（モデル継承ルール）を追記。
- **Lens 5 Breaking-change**: 2.1.198 に hook matcher / permission 照合 / glob 意味論の変更なし。出荷 `settings.json` は壊れない。

## 破壊的変更チェック

- **該当なし**。2.1.198 は Explore agent のモデル継承・background agent の自動化・多数の bugfix が中心で、hook `matcher` / permission-rule 照合の意味論は不変。テンプレ出荷 config（hook matcher・permission ルール）に影響なし → **壊れない**。

## トリアージ済みアクションリスト

### 🔴 必須

- [ ] #1: Explore agent = haiku 記載の訂正（v2.1.198+ は session モデル継承 [≤opus]）— `template/.claude/rules/subagent-delegation.md` / `template/README.md` / `template/.claude/rules/obsidian-mcp.md` + 根拠を `template/.claude/rules/ecc/common/performance.md`

### 🟡 推奨

- [ ] #2: `Notification` hook が background agent の待機/完了で発火（`agent_needs_input` / `agent_completed`, v2.1.198+）— `template/.claude/rules/ecc/common/hooks.md`
- [ ] #3: 2.1.198 主要変更を Important Notes に集約（Claude in Chrome GA・Explore モデル継承・subagent thinking 継承・background agent auto-commit/push/draft-PR・`/dataviz`・Notification 発火・transient エラー自動 retry）— `template/CLAUDE.md`

### ⚪ スキップ

- #4: `/agents` wizard 削除 — 理由: テンプレに `/agents` slash command 記載なし、無関係
- #5: anthropicAws failover / `awsAuthRefresh` — 理由: AWS 特有、大半のテンプレユーザーに無関係
- #6: plan mode read-only auto-allow / `.claude/rules/` symlink bugfix / 各種 UI bugfix（highlight.js 11・fullscreen・`/diff`・`/branch` 等）— 理由: 挙動改善・bugfix でドキュメント追従不要

## 変更詳細

### #1 Explore agent モデル継承（4 ファイル）

changelog 原文: *"The built-in Explore agent now inherits the main session's model (capped at opus) instead of running on haiku."*

- `subagent-delegation.md`: Delegation Matrix の `Explore | haiku` を `session model, ≤opus（v2.1.198+）` に訂正 + 脚注
- `README.md`: `Exploration → Explore agent (haiku)` を実態に訂正
- `obsidian-mcp.md`: `Explore subagent（haiku モデル）` を訂正
- `performance.md`: 「Agent モデル継承 (v2.1.198+)」小節を新設（Explore + subagent thinking 継承の根拠）

### #2 Notification hook

`hooks.md` の 27 events 表の `Notification` 行に、v2.1.198+ で background agent の待機/完了時に `agent_needs_input` / `agent_completed` で発火することを追記。

### #3 CLAUDE.md Important Notes

2.1.198 の主要変更を 1 bullet に集約して追記。
