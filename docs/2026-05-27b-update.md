# VibeCording Update — 2026-05-27b (v0.47.0)

## 変更点サマリー

Claude Code v2.1.152 (2026-05-27 リリース) で 追加された新機能を template に反映する minor 更新:

- **NEW**: `MessageDisplay` フックイベント (hook events 26 → 27)
- **NEW**: `/reload-skills` slash command
- **NEW**: Skill/slash command frontmatter `disallowed-tools`
- **NEW**: `SessionStart` フックの `reloadSkills: true` / `sessionTitle` 返り値
- **UPDATED**: `/code-review --fix` で findings を作業ツリーへ自動適用 (旧 `/simplify` は `/code-review --fix` のエイリアス化)
- **NEW**: `pluginSuggestionMarketplaces` enterprise managed setting
- **NEW**: `OTEL_METRICS_INCLUDE_ENTRYPOINT` env var
- **BEHAVIOR**: Auto mode はオプトイン同意不要に

ECC 2.0.0-rc.1 は **依然としてローカル未インストール**のため `ECC_PLUGIN_ROOT` は据え置き (1.10.0)。

## 環境スナップショット

- Local ECC installed: 1.10.0
- Template ECC version: 1.10.0 (unchanged)
- Current release: v0.46.1
- Next release: v0.47.0 (minor bump for new hook event + slash command)
- Local Claude Code: v2.1.150 (template 内 doc 上は v2.1.152 言及)

## 品質分析サマリー

| Item | Applicability | Cross-file | Risk | 判定 |
|---|---|---|---|---|
| `MessageDisplay` フックイベント | High | hooks.md (表ヘッダ + 行) | Low (additive) | 🔴 必須 |
| `/reload-skills` slash command | High | CLAUDE.md | Low | 🔴 必須 |
| `disallowed-tools` frontmatter (skill/slash command) | High | hooks.md または新セクション | Low | 🔴 必須 |
| `/code-review --fix` + `/simplify` redirect | High | CLAUDE.md | Low | 🟡 既存行更新 |
| `SessionStart` hook `reloadSkills: true` | Medium | hooks.md (新セクション) | Low | 🟡 推奨 |
| `SessionStart` hook `sessionTitle` | Medium | hooks.md (既存 v2.1.94+ 説明と統合) | Low | 🟡 推奨 |
| `pluginSuggestionMarketplaces` managed setting | Medium (enterprise) | README.md | Low | 🟡 推奨 |
| `OTEL_METRICS_INCLUDE_ENTRYPOINT` env var | Medium (OTEL ユーザー) | README.md | Low | 🟡 推奨 |
| Auto mode opt-in 撤廃 | Medium | CLAUDE.md Important Notes | Low | 🟡 推奨 |
| `/usage` 大型 session ファイル対応 | Low | CLAUDE.md 既存行軽微 | Low | ⚪ 既存行で十分 |
| Vim mode `/` 履歴検索 | Low | — | Low | ⚪ UX のみ |
| `--fallback-model` 自動切替え | Low | — | Low | ⚪ behavior のみ |
| Thinking summary 表示改善 | Low | — | Low | ⚪ UX のみ |
| `marketplace remove --scope` | Low | — | Low | ⚪ template 圏外 |
| 各種バグ修正 (15+ 件) | Low | — | Low | ⚪ runtime fix のみ |
| ECC 2.0.0-rc.1 | — | — | High | ⚪ ローカル未インストール |

## トリアージ済みアクションリスト

### 🔴 必須

- [ ] #1: `MessageDisplay` を `hooks.md` の 26 events 表に追加 → 27 events に変更
- [ ] #2: `/reload-skills` を `template/CLAUDE.md` Slash Commands リストに追加
- [ ] #3: `disallowed-tools` frontmatter を `hooks.md` 新セクションとして追加 (Skill/Slash command の挙動を runtime で制限)

### 🟡 推奨

- [ ] #4: `/code-review` 既存行を更新 (`--fix` 追加、`/simplify` → エイリアス化記述)
- [ ] #5: `SessionStart` hook 拡張 (`reloadSkills` / `sessionTitle`) を `hooks.md` Advanced Features に追加
- [ ] #6: `pluginSuggestionMarketplaces` を README.md Other Settings に追加
- [ ] #7: `OTEL_METRICS_INCLUDE_ENTRYPOINT` を README.md Env Vars に追加
- [ ] #8: Auto mode opt-in 撤廃を `template/CLAUDE.md` Important Notes に追記

### ⚪ スキップ

- ECC 2.0.0-rc.1 — ローカル未インストール (1.10.0 のみ)
- `/usage` 大型ファイル対応 — 既存 v2.1.149+ 行で機能名は同一、内部実装改善のみ
- Vim mode、`--fallback-model`、Thinking summary 表示、marketplace `--scope` — UX/behavior のみで template 設定なし
- v2.1.152 バグ修正 (15+ 件、cache_creation_input_tokens 等) — すべて runtime fix

## 変更詳細

### #1: `MessageDisplay` hook event 追加

**Source** (v2.1.152):
> Added a `MessageDisplay` hook event that lets hooks transform or hide assistant message text as it is displayed

**Before**: `## All Hook Events (26 events)` (`hooks.md:15`)

**After**: `## All Hook Events (27 events)` + 新行:
```markdown
| `MessageDisplay` | アシスタントメッセージ表示直前（テキストを変換/非表示にできる、v2.1.152+） | Yes |
```

### #2: `/reload-skills` 追加 (CLAUDE.md)

**Before**: 既存 `/reload-plugins` 行
**After**: 直下に追加:
```markdown
- `/reload-skills` — スキルディレクトリをセッション再起動なしで再スキャン (v2.1.152+)
```

### #3: `disallowed-tools` frontmatter 説明 追加 (hooks.md または別文書)

**Source**:
> Skills and slash commands can now set `disallowed-tools` in frontmatter to remove tools from the model while the skill is active

ベストロケーションは別文書を切るより `hooks.md` 末尾の Advanced Features セクションに新規 subsection として記載。理由: skill/slash command の制御フローと隣接して読まれる。

### #4: `/code-review` 既存行を更新

**Before**:
```markdown
- `/code-review [effort] [--comment]` — 現在の diff のバグを effort レベル指定でレビュー（v2.1.146+ で `/simplify` から名称変更）。`low|medium` は high-confidence findings のみ、`high|max` で broader coverage。`--comment` で GitHub PR にインラインコメント投稿（`/ultrareview` よりも軽量、ローカルブランチでも動作）
```

**After**:
```markdown
- `/code-review [effort] [--comment] [--fix]` — 現在の diff のバグを effort レベル指定でレビュー（v2.1.146+ で `/simplify` から名称変更、v2.1.152+ で `/simplify` は `/code-review --fix` のエイリアス）。`low|medium` は high-confidence findings のみ、`high|max` で broader coverage。`--comment` で GitHub PR にインラインコメント投稿。`--fix` で findings を作業ツリーに自動適用（reuse / simplification / efficiency 改善を提案）（v2.1.152+）
```

### #5: `SessionStart` hook `reloadSkills` / `sessionTitle` (hooks.md)

新セクション (v2.1.152 のすぐ近くに配置):
```markdown
### SessionStart hook 拡張: `reloadSkills` / `sessionTitle` (v2.1.152+)

`SessionStart` フックは以下 2 フィールドを返せる:
- `reloadSkills: true` — フックが新規 skill を install した直後、現セッションで再スキャンして利用可能化
- `hookSpecificOutput.sessionTitle` — 開始/再開時のセッションタイトル設定 (`UserPromptSubmit` 版 v2.1.94+ と同形式)
```

### #6: `pluginSuggestionMarketplaces` (README.md Other Settings)

**Source**:
> Added `pluginSuggestionMarketplaces` managed setting: admins can allowlist org marketplaces whose plugins may be suggested via context-aware tips

**After** (last row of Other Settings table):
```markdown
| `pluginSuggestionMarketplaces` | エンタープライズ managed 設定 — context-aware tips でプラグイン提案する組織 marketplace の allow リスト（v2.1.152+） |
```

### #7: `OTEL_METRICS_INCLUDE_ENTRYPOINT` (README.md Env Vars)

**Source**:
> Added the session entrypoint as an OpenTelemetry metric attribute (`app.entrypoint`, opt-in via `OTEL_METRICS_INCLUDE_ENTRYPOINT=true`)

**After** (Env Vars 表の末尾):
```markdown
| `OTEL_METRICS_INCLUDE_ENTRYPOINT` | OpenTelemetry metrics に `app.entrypoint`（セッション起動エントリ）属性を含める（v2.1.152+、opt-in） | `true` |
```

### #8: Auto mode opt-in 撤廃 (CLAUDE.md Important Notes)

**Source**:
> Auto mode no longer requires opt-in consent

簡潔に Important Notes に 1 行追加。

## 関連

- Source: <https://code.claude.com/docs/en/changelog>
- 前リリース: v0.46.1 (2026-05-27、allowAllClaudeAiMcps docs)
- 後続フォロー: ECC 2.x をローカルにインストールした際 `ECC_PLUGIN_ROOT` 更新と plugin 互換性確認
