# VibeCording Update — 2026-06-11

## 変更点サマリー

前回リリース v0.57.0（2026-06-10）以降の唯一の user-facing 版は Claude Code **2.1.172**（2.1.171 は欠番、2.1.165/167/168 は bug-fix のみ）。テンプレートは既に 2.1.170（Fable 5 / safe-mode / `/cd` / `disableBundledSkills`）まで反映済み。2.1.172 のうち設定テンプレ repo に適合するのは 2 点のみ:

1. **サブエージェントは最大 5 階層までネスト可能**（multi-agent オーケストレーションの能力上限）
2. **1M context セッションが標準上限を超えたときのネイティブ auto-compaction**（既出の `CLAUDE_CODE_AUTO_COMPACT_WINDOW` / #43989 ワークアラウンド記述に直結する安全網）

ECC は 2.0.0 が最新でローカルにも 2.0.0 のみインストール済み → **ECC バージョン変更なし**。🔴 必須項目なし。

## 環境スナップショット

- Local ECC installed: `2.0.0`（legacy dir に 1.10.0 残置）
- Template ECC version (`ECC_PLUGIN_ROOT`): `2.0.0`
- Current release: `v0.57.0`
- Latest Claude Code: `2.1.172`（2026-06-10）
- Latest ECC release: `v2.0.0`

## 品質分析サマリー

| 項目 | Lens 1 適合性 | Lens 2 横断 | Lens 3 リスク | Lens 4 完全性 | 判定 |
|---|---|---|---|---|---|
| サブエージェント 5 階層ネスト (2.1.172) | Medium（テンプレは subagent 委任を強く推奨） | `subagent-delegation.md` 単体で完結 | Low（追記のみ） | 自己完結 | 🟡 |
| 1M セッション auto-compaction (2.1.172) | High（既出の #43989 注意書きを更新） | `performance.md` + README env 行が同一トピック | Low（doc のみ、ワークアラウンドは残す） | 2 ファイルで一貫更新 | 🟡 |
| `availableModels` を subagent override/advisor に適用 (2.1.172) | Low（内部 hardening、テンプレ未記載の設定キー） | — | — | — | ⚪ |
| `WebFetch(domain:*)` / `Read(secrets-*/...)` ワイルドカード一致修正 (2.1.172) | Low（既存挙動の bug-fix） | — | — | — | ⚪ |
| Workflow の `Date.now()`/`Math.random()` 誤検出修正 (2.1.172) | Skip（テンプレは workflow script を同梱しない） | — | — | — | ⚪ |

> Lens 3 注意: 1M auto-compaction は #43989（1M モードで閾値が 400K に誤縮小、**OPEN/未修正**）を**解決しない**安全網にすぎない。ワークアラウンド（1M 利用者のみ `CLAUDE_CODE_AUTO_COMPACT_WINDOW=1000000`）の助言は残す。

## トリアージ済みアクションリスト

### 🔴 必須

- なし（ECC は既に最新 2.0.0、breaking な設定変更なし）

### 🟡 推奨

- [ ] #1: サブエージェント最大 5 階層ネスト — `template/.claude/rules/subagent-delegation.md`
- [ ] #2: 1M セッション auto-compaction の安全網を追記 — `template/.claude/rules/ecc/common/performance.md`（主） + `README.md` env 表 `CLAUDE_CODE_AUTO_COMPACT_WINDOW` 行（companion, Lens 2）

### ⚪ スキップ

- ECC バージョン更新 — 理由: 2.0.0 が最新かつローカル唯一のインストール版。変更不要
- `availableModels` の subagent/advisor 適用 — 理由: 内部 hardening / bug-fix。テンプレ未記載のキーで、ユーザーが設定する knob でもない
- `WebFetch`/`Read` permission ワイルドカード一致修正 — 理由: 既存挙動の bug-fix、doc 変更不要
- Workflow validation 修正（`Date.now()`/`Math.random()`） — 理由: bug-fix。テンプレは workflow script を同梱しない
- 2.1.172 のその他（Bedrock `~/.aws` region / OTEL `lines_of_code` model 属性 / `/plugin` 検索バー / agents-view・Remote Control・VSCode 系修正） — 理由: テンプレ非関連 or 純粋な bug-fix
- 2.1.165 / 2.1.167 / 2.1.168 — 理由: "Bug fixes and reliability improvements" のみ

## 変更詳細

### #1 サブエージェント 5 階層ネスト

`template/.claude/rules/subagent-delegation.md` の冒頭付近に 1 行追記:

> サブエージェントは最大 5 階層までネストして起動できる（2.1.172+）。深い委任ツリーを設計するときの上限として把握しておく。

### #2 1M セッション auto-compaction

**`performance.md` §自動圧縮の閾値設定** に bullet 追記（before/after）:

Before（末尾の bullet）:
```
- `=1000000` は **1M context モード…** を回避する **opt-in workaround**。標準 200K ウィンドウの利用者は設定不要…
```

After（その後ろに 1 bullet 追加）:
```
- v2.1.172+: 1M context セッションが標準上限を超えると **ネイティブ auto-compaction** が発動する安全網が入った。ただし #43989（1M モードで閾値が 400K に誤縮小）自体は **未修正 OPEN** のため、1M 利用者のワークアラウンドは引き続き有効。
```

**README.md env 表 `CLAUDE_CODE_AUTO_COMPACT_WINDOW` 行** の説明末尾に追記:

> （v2.1.172+ で 1M セッションが標準上限超過時にネイティブ auto-compaction が発動する安全網が追加されたが、#43989 自体は未修正のためワークアラウンドは継続）
