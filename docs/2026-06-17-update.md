# VibeCording Update — 2026-06-17

## 変更点サマリー

Claude Code **2.1.179** 対応のカバレッジ記録リリース。2.1.179 は **全 9 項目が runtime バグ修正**で、新規 settings key / environment variable / hook event・field / CLI flag / slash command / model ID / agent・skill 変更は一切なし。テンプレート（設定リポジトリ）に適用すべき変更はゼロのため、**テンプレ/README ファイルは無変更**、本 docs 更新ノートのみ追加する doc-only リリース。ECC は **2.0.0** 据え置き（ローカル＝テンプレ＝2.0.0、変更なし）。

## 環境スナップショット

- Local ECC installed: `2.0.0`（legacy dir に `1.10.0` も残存）
- Template ECC version: `2.0.0`（`ECC_PLUGIN_ROOT` = `${HOME}/.claude/plugins/cache/everything-claude-code/ecc/2.0.0`）
- Current release: `v0.61.0`（Claude Code 2.1.177–2.1.178 対応）
- Latest Claude Code: `2.1.179`（2026-06-16 公開）

## 品質分析サマリー

2.1.179 の 9 項目すべてを 4 レンズで評価。

- **Lens 1（適合性）**: 9 項目すべて runtime バグ修正で、ユーザーが設定・記述する新 capability ではない → 全項目 **Skip**。
- **Lens 2（横断一貫性）**: 文書化する項目がないため対象外。
- **Lens 3（導入リスク）**: 文書化候補ゼロ。テンプレ無変更のため既存ユーザーへの破壊リスクなし（Low）。
- **Lens 4（適用完全性）**: テンプレ/README に該当記述なし。`denyRead`/`allowRead` はテンプレ未使用（`sandbox.failIfUnavailable` のみ）。WSL2 マウスホイールスクロールは未文書化。Ctrl+O の subagent transcript 修正は既存記述（「ノーマル/詳細トランスクリプト切り替え」「Verbose mode: Ctrl+O」）と矛盾せず additive。→ 追加タスクなし。

結論: **適用変更ゼロ**。バージョン追跡の連続性維持のため、本ノートで 2.1.179 を「検証済み・テンプレ変更なし」として記録する。

## トリアージ済みアクションリスト

### 🔴 必須

なし。

### 🟡 推奨

なし。

### ⚪ スキップ（全 9 項目）

- #1: mid-stream 接続断で部分応答を保持＋spinner が "running tool" で固着しない — 理由: runtime fix、テンプレ config に影響なし
- #2: WSL2（Windows Terminal / VS Code）マウスホイールスクロール修正（2.1.172 regression） — 理由: runtime fix、WSL2 scroll は未文書化（テンプレは fullscreen の `wheelScrollAccelerationEnabled` のみ言及）
- #3: sandbox `denyRead`/`allowRead` の大規模ディレクトリツリー glob で Bash ツール説明が肥大し Linux でセッション使用不能になる不具合 — 理由: runtime fix、テンプレは `sandbox.failIfUnavailable` のみで `allowRead`/`denyRead` キー不使用
- #4: feedback survey がターン直後の一桁返信を session rating と誤認する不具合 — 理由: runtime UX fix
- #5: welcome 画面で販促バナーが重複表示される不具合（1 セッション最大 1 件に） — 理由: runtime UX fix
- #6: subagent 表示時に Ctrl+O が subagent transcript を表示しない不具合 — 理由: additive な fix、既存 Ctrl+O 記述と矛盾せず文書化不要
- #7: prompt 入力クリックで subagent/footer パネルから focus が戻らない不具合 — 理由: runtime UX fix
- #8: remote session の background タスクがターン間で "still running" のまま固着して見える不具合 — 理由: runtime fix、remote session 詳細は未文書化
- #9: remote session のプラグイン読込性能改善 — 理由: perf 改善、テンプレ config に影響なし

## 変更詳細

テンプレート・README への変更なし。本リリースは Claude Code 2.1.179 を検証し、テンプレートへの適用変更が不要であることを記録するカバレッジ記録リリース。

### 検証ソース

- Claude Code changelog（`raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md`）— 2.1.179 が最新エントリ、それ以降なし
- `gh release view v2.1.179 -R anthropics/claude-code` — 9 項目すべて "Fixed" / "Improved" のバグ修正・性能改善
- `gh release list -R affaan-m/everything-claude-code` — 最新 `v2.0.0`（テンプレ据え置きと一致）
