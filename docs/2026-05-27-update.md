# VibeCording Update — 2026-05-27 (v0.46.1)

## 変更点サマリー

Claude Code v2.1.149 で追加された **enterprise managed setting `allowAllClaudeAiMcps`** を README.md の Other Settings 表に追記。v2.1.150 は internal infrastructure のみで user-facing 変更なし。

ECC は 2.0.0-rc.1 が GitHub にリリースされているが、ローカルには 1.10.0 のみインストール済みのため、`ECC_PLUGIN_ROOT` の更新は実施しない (install.sh が disk から自動検出する設計のため、未インストール版に書き換えると hook が壊れる)。

## 環境スナップショット

- Local ECC installed: 1.10.0
- Template ECC version: 1.10.0 (unchanged)
- Current release: v0.46.0
- Next release: v0.46.1 (docs-only patch)

## 品質分析サマリー

| Item | Applicability | Cross-file | Risk | Completeness | 判定 |
|---|---|---|---|---|---|
| `/usage` per-category breakdown (v2.1.149) | High | — | Low | ✓ 既に CLAUDE.md:63 に記載 | ⚪ 既適用 |
| `/diff` キーボードスクロール (v2.1.149) | Low | — | Low | — | ⚪ template に config なし |
| GFM タスクリストチェックボックス描画 (v2.1.149) | Low | — | Low | — | ⚪ template に config なし |
| `allowAllClaudeAiMcps` managed setting (v2.1.149) | Medium (enterprise) | README only | Low | ❌ 未記載 | 🟡 追加 |
| v2.1.149 各種バグ修正 (PowerShell `cd..` 等) | Low | — | Low | — | ⚪ バグ修正のみ |
| v2.1.150 internal infrastructure | Low | — | Low | — | ⚪ user-facing なし |
| ECC 2.0.0-rc.1 | — | — | High | — | ⚪ ローカル未インストール |

## トリアージ済みアクションリスト

### 🔴 必須

(なし — `settings.json` / hooks / 既存 rule に必要な変更なし)

### 🟡 推奨

- [ ] #1: `allowAllClaudeAiMcps` enterprise managed setting を README.md の Other Settings 表に追加

### ⚪ スキップ

- ECC 2.0.0-rc.1 — ローカルに未インストール (1.10.0 のみ)。install.sh は disk から auto-detect するため、未インストール版に切り替えると hook が壊れる
- `/diff` キーボードスクロール — UI 操作のみで template 設定なし
- GFM タスクリスト描画 — Markdown 描画改善のみで template 設定なし
- PowerShell `cd..` 権限バイパス修正 — runtime bug fix
- find 大規模ディレクトリでのクラッシュ修正 — runtime bug fix
- managed-settings 承認ダイアログ凍結修正 — runtime bug fix
- その他 v2.1.149 のバグ修正 (15+ 件) — すべて runtime bug fix で template 設定への影響なし
- v2.1.150 — internal infrastructure improvements only

## 変更詳細

### #1: README.md "Other Settings" 表に `allowAllClaudeAiMcps` 追加

**Source** (v2.1.149 changelog):
> Enterprise: added the `allowAllClaudeAiMcps` managed setting to load claude.ai cloud MCP connectors alongside `managed-mcp.json`

**Before** (README.md:277 付近):
```markdown
| `autoMode.hard_deny` | auto モード分類ルール — ユーザーの意図や allow 例外に関わらず無条件ブロック（v2.1.136+） |
```

**After**:
```markdown
| `autoMode.hard_deny` | auto モード分類ルール — ユーザーの意図や allow 例外に関わらず無条件ブロック（v2.1.136+） |
| `allowAllClaudeAiMcps` | エンタープライズ managed 設定 — `managed-mcp.json` と並んで claude.ai クラウド MCP コネクタをロード（v2.1.149+） |
```

## 関連

- Source: <https://code.claude.com/docs/en/changelog>
- 前リリース: v0.46.0 (2026-05-26、strip-memory-provenance hook)
- 次回フォローアップ予定: ECC 2.x がローカルにインストールされた段階で `ECC_PLUGIN_ROOT` 更新と plugin 互換性確認
