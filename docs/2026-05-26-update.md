# VibeCording Update — 2026-05-26

## 変更点サマリー

Claude Code v2.1.147〜v2.1.150 の薄い更新サイクル。最大の追加は v2.1.147 の **Pinned background sessions** と v2.1.149 の **`/usage` カテゴリ別内訳**。`/simplify` → `/code-review` リネームは v0.43.0 で既に反映済み。ECC 2.0.0-rc.1 はローカル未インストールにつきスキップ（テンプレートを未インストール版に上げると `install.sh` の自動検出が外れて hooks が壊れる）。

## 環境スナップショット
- Local ECC installed: 1.10.0
- Template ECC version: 1.10.0
- Current release: v0.44.2

## 品質分析サマリー

### Lens 1 — Applicability
| Change | Score | 判定 |
|---|---|---|
| v2.1.147 Pinned bg sessions (`Ctrl+T`) | Medium | 既に `claude --bg` を hooks.md で言及済 → CLAUDE.md 1 行追加 |
| v2.1.147 `/simplify` → `/code-review` rename | — | 既に CLAUDE.md L61 で文書化 |
| v2.1.148 Bash exit 127 regression | Skip | 内部バグ修正 |
| v2.1.149 `/usage` per-category breakdown | Low/Medium | 既存 `/usage` 行に追記で対応 |
| v2.1.149 `/diff` keyboard scrolling | Skip | キーバインド詳細、テンプレ的価値なし |
| v2.1.149 GFM task list checkbox rendering | Skip | 出力レンダリングのみ |
| v2.1.149 `allowAllClaudeAiMcps` enterprise managed setting | Skip | エンタープライズ管理設定、個人テンプレ対象外 |
| v2.1.150 infrastructure | Skip | 内部のみ |
| ECC 2.0.0-rc.1 | Skip | 未インストール — `ECC_PLUGIN_ROOT` を未インストール版に向けると hooks が落ちる |

### Lens 2 — Cross-file Consistency
- Pinned bg sessions: `template/CLAUDE.md` に 1 行。`hooks.md` の `background_tasks` 記述 (v2.1.145+) と整合。
- `/usage` 拡張: `template/CLAUDE.md` のスラッシュコマンド行を更新するだけ。

### Lens 3 — Risk Assessment
全て documentation-only、Low risk。

### Lens 4 — Completeness
- `/simplify` → `/code-review` は v0.43.0 で `(v2.1.146+ で /simplify から名称変更)` と記載済。実際の rename 完了は v2.1.147 だが、v2.1.146 で機能利用可能だった事実を踏襲しているので変更不要。
- 過去サイクルで未反映の取りこぼしは無し。

## トリアージ済みアクションリスト

### 🔴 必須
- なし

### 🟡 推奨
- [ ] #1: v2.1.147 Pinned background sessions の存在を CLAUDE.md Important Notes に追記
- [ ] #2: v2.1.149 `/usage` per-category breakdown を CLAUDE.md `/usage` 行に追記

### ⚪ スキップ
- ECC 2.0.0-rc.1: ローカル未インストール — `install.sh` は disk から自動検出するので、テンプレートを未インストール版に上げると hooks 起動失敗
- v2.1.147 `/simplify` → `/code-review` rename: 既に文書化済 (CLAUDE.md L61)
- v2.1.148 Bash exit 127: 内部バグ修正、テンプレ無関係
- v2.1.149 `/diff` keyboard scrolling / GFM checkbox / `allowAllClaudeAiMcps`: テンプレ対象外（UX 詳細 / 出力レンダリング / エンタープライズ管理）
- v2.1.150: 内部 infra のみ

## 変更詳細

### #1: Pinned background sessions (v2.1.147)

`template/CLAUDE.md` の Important Notes に追記:

```markdown
- `claude --bg` で起動した bg セッションは `claude agents` ビューで `Ctrl+T` によりピン留め可能（v2.1.147+）。ピン留めセッションはアイドル時も維持、メモリ圧迫時も非ピン留めが先に shed される
```

### #2: `/usage` per-category breakdown (v2.1.149)

`template/CLAUDE.md` L63 を更新:

before:
```markdown
- `/usage` — トークン使用量とコストを表示（`/cost` + `/stats` の統合版）
```

after:
```markdown
- `/usage` — トークン使用量とコストを表示（`/cost` + `/stats` の統合版）。v2.1.149+ で skills/subagents/plugins/MCP サーバー別の上限消費内訳を表示
```
