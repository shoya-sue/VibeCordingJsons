# VibeCording Update — 2026-05-12

## 変更点サマリー

Claude Code v2.1.139 (2026-05-11) の新機能を反映。
`/goal` スラッシュコマンド、フック `args[]` exec形式、`continueOnBlock` オプション、`CLAUDE_PROJECT_DIR` 環境変数を追加。

## 環境スナップショット

- Local ECC installed: 1.10.0
- Template ECC version: 1.10.0 (変更なし)
- Current release: v0.32.7

## 品質分析サマリー

### Lens 1 — Applicability（テンプレートとしての適合性）

| # | 変更 | スコア | 理由 |
|---|------|--------|------|
| 1 | `/goal` スラッシュコマンド | High | テンプレートユーザーが直接使う |
| 2 | `args: string[]` hook exec形式 | High | カスタムフック作者に有用 |
| 3 | `continueOnBlock` PostToolUse | High | フック制御の重要オプション |
| 4 | `CLAUDE_PROJECT_DIR` env var | Medium | MCP/フック作者に有用 |
| 5 | `claude agents` CLI | Medium | AGENTS.md文脈で記載価値あり |
| 6 | `/scroll-speed` スラッシュコマンド | Low→Skip | UI好み設定、テンプレート不要 |
| 7 | VSCode `enableReopenClosedSessionShortcut` | Skip | IDE特化、テンプレートスコープ外 |
| 8 | Remote Control + API key 挙動変化 | Skip | 設定変更なし、行動的注意事項 |

### Lens 2 — Cross-file Consistency（横断的一貫性）

- `/goal` → `template/CLAUDE.md` Slash Commands セクション
- `args: string[]` → `template/.claude/rules/ecc/common/hooks.md` + `README.md` hooks表
- `continueOnBlock` → 同上
- `CLAUDE_PROJECT_DIR` → `README.md` env表のみ（hooks.mdには既存 effort.level 節があるが別の概念）
- `claude agents` → `template/AGENTS.md` Features セクション

### Lens 3 — Risk Assessment（導入リスク）

全項目Low：既存設定の変更なし、純粋に追加ドキュメント。

### Lens 4 — Completeness（適用完全性）

- `args[]` / `continueOnBlock` は hooks.md と README 両方に記載が必要（両者がフック機能を説明）
- `Skill(*)` ワイルドカード修正 (v2.1.139) は既にテンプレートに `"Skill(*)"` が存在するため対応不要

## トリアージ済みアクションリスト

### 🔴 必須

- [ ] #1: `/goal` スラッシュコマンド追加 — `template/CLAUDE.md`
- [ ] #2: フック `args: string[]` exec形式 追加 — `template/.claude/rules/ecc/common/hooks.md`
- [ ] #3: `continueOnBlock` PostToolUse オプション 追加 — `template/.claude/rules/ecc/common/hooks.md`
- [ ] #4: README hooks.md と同期（#2/#3の companion） — `README.md`

### 🟡 推奨

- [ ] #5: `CLAUDE_PROJECT_DIR` env var 追加 — `README.md` env表
- [ ] #6: `claude agents` CLI 記載 — `template/AGENTS.md`

### ⚪ スキップ

- #7: `/scroll-speed` — 理由: UI好み設定、テンプレートスコープ外
- #8: `claudeCode.enableReopenClosedSessionShortcut` — 理由: VSCode IDE固有設定
- #9: Remote Control + API key 挙動 — 理由: 設定変更なし、テンプレート非対応機能

## 変更詳細

### #1: `/goal` スラッシュコマンド (template/CLAUDE.md)

**Before:** `/usage` 行の後ろに追加
**After:** Slash Commands セクションに以下を追加:
```
- `/goal <condition>` — 完了条件を設定、条件達成まで複数ターンで継続実行
```

### #2: `args: string[]` exec形式 (hooks.md)

Advanced Features セクションに追加:
```markdown
### コマンド exec 形式 (`args: string[]`, v2.1.139+)

`args` フィールドを使うとシェルを経由せず直接実行（パス注入不要）:

\`\`\`jsonc
{
  "type": "command",
  "args": ["node", "/path/to/script.js"],   // シェルなし直接起動
  "timeout": 10
}
\`\`\`
```

### #3: `continueOnBlock` (hooks.md)

PostToolUse `continueOnBlock` オプション:
```markdown
### PostToolUse `continueOnBlock` (v2.1.139+)

PostToolUse フックがブロック（`"continue": false`）したとき、デフォルトはターンを終了。
`continueOnBlock: true` を設定するとブロック理由を Claude に返してターンを継続できる:

\`\`\`jsonc
{
  "matcher": "Write|Edit",
  "continueOnBlock": true,
  "hooks": [...]
}
\`\`\`
```

### #4: README hooks.md と同期 (README.md)

READMEのhooks表にある「5 hook types」の説明文を更新（`args[]` / `continueOnBlock` を追記）

### #5: `CLAUDE_PROJECT_DIR` (README.md env表)

| `CLAUDE_PROJECT_DIR` | MCP stdio サーバーに自動設定されるプロジェクトルート（v2.1.139+）| (auto) |

### #6: `claude agents` CLI (AGENTS.md)

Features > Claude Code セクションに `claude agents` コマンドを記載
