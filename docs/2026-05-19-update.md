# VibeCording Update — 2026-05-19

## 変更点サマリー

Claude Code v2.1.144 (2026-05-19) の変更を反映。本リリースは主にバグ修正が中心で、テンプレートに直接影響する新規機能は少ない。可視機能として `/model` のセッションスコープ化（押下した `/model` は現セッション限定、`d` キーで全体デフォルト設定）を template/CLAUDE.md の `/model opusplan` 行に注記する。`/usage-credits` rename と `/resume` の bg マーカー対応はテンプレートに該当エントリがないためスキップ。ECC バージョン変更なし (v1.10.0 のまま)。

## 環境スナップショット

- Local ECC installed: 1.10.0
- Template ECC version: 1.10.0 (in sync — no ECC update needed)
- Current release: v0.39.0
- Next version: v0.40.0
- Local Claude Code: v2.1.144

## 品質分析サマリー

研究対象: Claude Code v2.1.144（2026-05-19、v0.39.0 リリース以降の唯一の新バージョン）。ECC は v1.10.0 が最新で変更なし（直近 4/5 リリースから新規なし）。

### 候補変更リスト（pre-analysis）

| # | 変更内容 | バージョン |
|---|---------|----------|
| 1 | `/model` がセッション限定の挙動に変更（`d` キー押下で新セッションのデフォルト設定） | v2.1.144 |
| 2 | `/usage-credits` が `/extra-usage` から rename（旧名も継続動作） | v2.1.144 |
| 3 | `/resume` がバックグラウンドセッションをサポート（`bg` マーカー付きで表示） | v2.1.144 |
| 4 | バックグラウンドセッションが起床後に model/effort を保持 | v2.1.144 |
| 5 | macOS Full Disk Access 保護フォルダ配下の background session "exit 1 before init" クラッシュ修正 | v2.1.144 |
| 6 | MCP `tools/list` paginated レスポンスの 1 ページ目以降欠落バグ修正 | v2.1.144 |
| 7 | api.anthropic.com 到達不可時の起動最大 75 秒ハング → 15 秒タイムアウト化 | v2.1.144 |
| 8 | その他多数のバグ修正（terminal corruption、Bedrock/Vertex Opus 1M 選択、`/branch` worktree、headless mode の Skill 権限エラー等） | v2.1.144 |

### Lens 1 — Applicability

| # | スコア | 理由 |
|---|--------|------|
| 1 | **Medium** | template/CLAUDE.md line 45 で `/model opusplan` を明示推奨しており、「一度設定すれば永続化される」前提が崩れる。`d` キーでの default 設定を簡潔に注記する価値あり |
| 2 | Skip | 旧 `/extra-usage` も新 `/usage-credits` もテンプレートに記載なし |
| 3 | Low | `/resume` 自体がテンプレートに記載なし。`bg` マーカーは操作的 UX |
| 4 | Low | 操作的 UX（バグ修正寄り）。設定不要 |
| 5 | Skip | バグ修正、テンプレ可視機能に影響なし |
| 6 | Skip | バグ修正、テンプレ可視機能に影響なし |
| 7 | Skip | バグ修正、テンプレ可視機能に影響なし |
| 8 | Skip | バグ修正のみ |

### Lens 2 — Cross-file Consistency

- #1 (`/model` セッションスコープ化): `template/CLAUDE.md` line 45（`/model opusplan` 行）に注記を追加。README.md にも `/model` の解説（line 344）があるが、`opusplan` ワークフローを推奨しているのみで session-scope の議論はない。短い注記を README.md にも併記すると親切（Lens 2 対象に追加）

### Lens 3 — Risk Assessment

- #1: **Low**（追加注記のみ、既存設定不変、`/model opusplan` 自体の動作は不変、user は `d` を押すという追加情報を得るだけ）

### Lens 4 — Completeness

- v0.39.0 までに v2.1.143 までを反映済み
- v2.1.144 の可視機能は #1 のみ反映対象
- ECC v1.10.0 据置 → `ECC_PLUGIN_ROOT` 変更不要

### 最終トリアージ

🔴 必須: なし（ECC バージョン変更なし、破壊的変更なし）
🟡 推奨: #1a (template/CLAUDE.md 注記), #1b (README.md 注記)
⚪ スキップ: #2, #3, #4, #5, #6, #7, #8

## トリアージ済みアクションリスト

### 🔴 必須

（なし）

### 🟡 推奨

- [ ] #1a: `/model` のセッションスコープ注記を `/model opusplan` 行に追加 — `template/CLAUDE.md`
- [ ] #1b: 同じ注記を Model Selection セクションに 1 行追加 — `README.md`

### ⚪ スキップ

- #2: `/usage-credits` rename — 理由: 旧名・新名ともテンプレ未記載
- #3: `/resume bg` マーカー — 理由: `/resume` がテンプレ未記載、操作的 UX
- #4: BG セッション model/effort 保持 — 理由: 操作的 UX、設定不要
- #5–#8: バグ修正各種 — 理由: テンプレート内容に影響なし

## 変更詳細

### #1a: `/model` セッションスコープ注記 — `template/CLAUDE.md`

**before** (line 45):

```
- `/model opusplan` — Auto-switch: Opus for planning, Sonnet for execution
```

**after**:

```
- `/model opusplan` — Auto-switch: Opus for planning, Sonnet for execution（v2.1.144+ で `/model` はセッション限定、ピッカーで `d` キーを押すと新セッションのデフォルトに設定）
```

### #1b: 同注記を Model Selection セクションに追加 — `README.md`

`/model` セクション（line 344 付近）の `/model opusplan workflow` 直前または直後に 1 文追加:

```
> **v2.1.144+ 挙動変更**: `/model` で選んだモデルは現セッションのみ適用。新セッションのデフォルトにしたい場合はモデルピッカーで `d` キーを押す。
```

## Sources

- [Claude Code Changelog](https://code.claude.com/docs/en/changelog)
- [anthropics/claude-code releases](https://github.com/anthropics/claude-code/releases)
- [affaan-m/everything-claude-code releases](https://github.com/affaan-m/everything-claude-code/releases) — no new release (still v1.10.0, latest 2026-04-05)
